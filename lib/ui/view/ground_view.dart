import 'package:flutter/material.dart';

class GroundView extends StatelessWidget {
  const GroundView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
